@model IEnumerable<First_Project.Models.About>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ImageOne)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageTwo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParagraphOne)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParagraphTwo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ImageOne)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageTwo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParagraphOne)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParagraphTwo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
